site_name: coiled
site_description: Coiled utils
site_author: Nehal Patel
repo_url: https://github.com/habemus-papadum/coiled
repo_name: coiled

theme:
  name: "material"
  palette:
    primary: blue grey


nav:
  - Home: index.md
  - API: reference.md

# If you'd like to specify a separate docs directory, you can do so here
docs_dir: docs

# Plugins to enable
plugins:
  - search
  - mkdocs-jupyter
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          options:
            docstring_style: numpy
            show_submodules: true
